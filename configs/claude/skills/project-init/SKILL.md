---
name: project-init
description: Initialize a new project with standard scaffolding (git, GitHub repo, LICENSE, README, CI, repo audit). Use when the user asks to create a new project, start a new repo, or scaffold a codebase. Language-specific skills (npm-package, swift-project, rust-project) load this skill automatically — also works standalone for languages without a dedicated skill.
---

# Project Initialization

Follow the steps in `common.md` for the full initialization workflow.

For language-specific setup, use the appropriate skill after init:
- `/npm-package` — TypeScript/Node.js packages
- `/swift-project` — Swift apps, libraries, CLI tools
- `/rust-project` — Rust crates and workspaces
