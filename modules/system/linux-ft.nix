# ft (42 school Linux) platform-specific config.
#
# Intentionally empty placeholder — kept so future ft-only tweaks have an
# obvious home (the ft `p` alias opens this file). Not dead code.
{ ... }: { }
